Solution of the first and second task are located in answewrs.md .
For the third task, my sollution is in setup.sh and dicom_playbook.yml.

## Third task assumptions
For the bash script to work properly you need to run it on Linux distribution with `apt` package installer. You also need sudo permissions.

## How to run the script
Paste the following commands into the terminal:
```bash
chmod +x setup.sh
./setup.sh
```